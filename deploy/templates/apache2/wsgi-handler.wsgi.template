import glob
import os
import site
import sys

yolapi_install_root = "{{conf.deploy.install_path}}"

sys.path.append("%s" % yolapi_install_root)
sys.path.append("%s/yolapi" % yolapi_install_root)
for dir_ in glob.glob("%s/lib/python2.*/site-packages" % yolapi_install_root):
    site.addsitedir(dir_)
os.environ['DJANGO_SETTINGS_MODULE'] = 'yolapi.settings'

import django.core.handlers.wsgi
application = django.core.handlers.wsgi.WSGIHandler()
